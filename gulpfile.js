let gulp = require('gulp');
let webpack = require('webpack-stream');

gulp.task('build', build);

/**
 * Builds the software
 */
function build() {
    // return gulp.src()
    //            .pipe(webpack()) ;
}