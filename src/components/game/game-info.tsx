import * as React from 'react';
import { Switch, Route } from 'react-router';

export interface IGameInfoProps {  }
export interface IGameInfoState {  }

export class GameInfo extends React.Component<IGameInfoProps, IGameInfoState> {
    render() {
        return (
            <div>
                
            </div>
        );
    }
}